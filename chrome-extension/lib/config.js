// Same project the web app uses (see ../../.env.local). The anon key is
// meant to be public — it's already embedded in the web app's client
// bundle — Row Level Security, not secrecy, is what protects the data.
export const SUPABASE_URL = "https://kegtulkvmdudvdbqixwc.supabase.co";
export const SUPABASE_ANON_KEY = "sb_publishable_mDJ4o0gfrZk7vrrq3ljOKw_Fe_0iZnm";
// Update this to the deployed domain in production.
export const DASHBOARD_URL = "http://localhost:3000/dashboard";
